<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.biz.bbs.mapper.FinalDao">

	<insert id="insert">
		INSERT INTO tbl_notice(
		SEQ,
		ID,
        DATE_TIME,
        WRITER,
        SUBJECT,
        TEXT 
		) VALUES (
		SEQ_NOTICE.NEXTVAL,
		#{id},
		#{date_time},
		#{writer},
		#{subject},
		#{text}
		)
	</insert>
	<update id="update">
		UPDATE tbl_notice SET	
		id = #{id},
		date_time = #{date_time},
		writer = #{writer},
		subject = #{subject},
		text = #{text}
		WHERE seq = #{seq}


	</update>
</mapper>